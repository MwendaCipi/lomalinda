"use client";
import { FormEvent, useState } from "react";
const API_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000";
export default function CommunityPrayerPage() {
  const [requestText, setRequestText] = useState(""); const [message, setMessage] = useState("");
  async function submitRequest(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const response = await fetch(`${API_URL}/api/members/prayer-requests/`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ request_text: requestText, anonymous: false }) }); setMessage(response.ok ? "Your prayer request has been received. We will pray with you." : "We could not submit your request. Please try again."); if (response.ok) setRequestText(""); }
  return <main className="min-h-screen bg-[#f7f4ee] px-6 pt-10 pb-8 text-[#26352f] sm:py-12"><div className="mx-auto max-w-3xl"><h1 className="text-3xl font-semibold tracking-tight sm:text-5xl">Prayer</h1><p className="mt-3 text-lg leading-8 text-[#617068]">You do not have to carry it alone.</p><p className="mt-2 text-sm leading-6 text-[#617068]">Bring what is on your heart. Our church family would be honoured to pray with you.</p><form onSubmit={submitRequest} className="mt-5 rounded-3xl bg-white p-5 shadow-sm ring-1 ring-[#dfdbd1] sm:p-7"><label className="block text-sm font-medium">Your prayer request<textarea required rows={4} value={requestText} onChange={(event) => setRequestText(event.target.value)} className="mt-2 w-full rounded-xl border border-[#c9c5bb] px-4 py-3" placeholder="Share what is on your heart..." /></label><button className="mt-4 w-full rounded-full bg-[#b36b3c] px-6 py-3 font-semibold text-white">Send prayer request</button>{message && <p className="mt-4 rounded-xl bg-[#eef2ed] p-4 text-sm">{message}</p>}</form><blockquote className="mt-6 border-l-2 border-[#b36b3c] pl-6 text-lg leading-7 text-[#3d5148]">“Prayer is the opening of the heart to God as to a friend.”<footer className="mt-2 text-sm font-semibold text-[#b36b3c]">— Ellen G. White, <cite>Steps to Christ</cite></footer></blockquote></div></main>;
}
